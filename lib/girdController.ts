// gridController.ts

class gridController {
  private gridSize: number;
  public gridLabel: string;

  constructor(gridSize: number, gridLabel: string) {
    this.gridSize = gridSize;
    this.gridLabel = gridLabel;
  }
}
